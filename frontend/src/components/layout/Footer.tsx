export default function Footer() {
  return (
    <footer className="px-8 py-6 border-t border-hairline mt-auto">
      <p className="font-mono text-xs text-mute">
        ResumeScan — built to read what the filter reads.
      </p>
    </footer>
  );
}
